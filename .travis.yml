language: minimal

notifications:
  email: false

branches:
  only:
    - master

os:
  - linux

dist:
  - bionic

env:
  global:
    - PATH=$PATH:$HOME/bin

before_install:
  - sudo apt update && sudo apt install cc65
  - mkdir bin && cd bin && curl -OL -u "travis:545393775684" https://ejona.ersoft.org/taus-ci/flips
  - cd taus && curl -OL -u "travis:545393775684" https://ejona.ersoft.org/taus-ci/tetris.nes

install:
  - make V=1
