language: minimal

notifications:
  email: false

branches:
  only:
    - master

os:
  - linux

dist:
  - bionic

env:
  global:
    - PATH=$PATH:$HOME/bin

before_install:
  - sudo apt-get install cc65
  - ls $HOME/bin
  - cd $HOME/bin && curl -OL -u "travis:545393775684" https://ejona.ersoft.org/taus-ci/flips
  - curl -OL -u "travis:545393775684" https://ejona.ersoft.org/taus-ci/tetris.nes

install:
  - make V=1
